#include <stdio.h>

int main()
{
    int n, count = 0;

    printf("Enter Number: ");
    scanf("%d", &n);

    while(n != 0)
    {
        count++;
        n /= 10;
    }

    printf("Number of Digits = %d", count);

    return 0;
}
Sample Output
Enter Number: 12345
Number of Digits = 5
