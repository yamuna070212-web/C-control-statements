#include <stdio.h>

int main()
{
    int age;

    printf("Enter Age: ");
    scanf("%d", &age);

    if(age >= 18)
        printf("Eligible to Vote");
    else
        printf("Not Eligible");

    return 0;
}
Sample Output
Enter Age: 19
Eligible to Vote
