#include <stdio.h>

int main()
{
    int n, i, sum = 0;

    printf("Enter N: ");
    scanf("%d", &n);

    for(i = 2; i <= n; i += 2)
    {
        sum += i;
    }

    printf("Sum = %d", sum);

    return 0;
}
Sample Output
Enter N: 10
Sum = 30
